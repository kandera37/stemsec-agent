{
  "findings": [
    {
      "type": "sql_injection",
      "severity": "high",
      "confidence": "high",
      "evidence": "The SQL query is constructed using string interpolation with user input 'q': query = f\"SELECT * FROM products WHERE name LIKE '%{q}%'\"",
      "recommendation": "Use parameterized queries to safely include user input in SQL statements, e.g., cursor.execute(\"SELECT * FROM products WHERE name LIKE ?\", ('%' + q + '%',))"
    }
  ],
  "summary": "The code is vulnerable to SQL injection due to direct string interpolation of user input into the SQL query without parameterization. This allows an attacker to manipulate the query structure. No other security issues were identified."
}